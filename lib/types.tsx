export type Employee = {
    id: number;
    name: string;
    hourly_rate: number;
    active: boolean;
  };

  export type Client = {
    id: number;
    name: string;
    email: string;
  }
  export type Project = {
    id: number
    name: string
}